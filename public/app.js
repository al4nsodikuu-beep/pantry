'use strict';
const icons = {
 leaf:'<path d="M5 17C1 7 12 3 21 3c0 10-4 18-13 15M4 21 17 8"/>',
 grid:'<rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/>',
 book:'<path d="M4 4h7a3 3 0 0 1 3 3v14a4 4 0 0 0-3-2H4zM14 7a3 3 0 0 1 3-3h4v15h-4a4 4 0 0 0-3 2"/>',
 sparkles:'<path d="m12 3 2.5 6.5L21 12l-6.5 2.5L12 21l-2.5-6.5L3 12l6.5-2.5L12 3ZM20 2v4m-2-2h4M3 18v4m-2-2h4"/>',
 camera:'<path d="M8 6 9.5 3h5L16 6h4a2 2 0 0 1 2 2v11H2V8a2 2 0 0 1 2-2Z"/><circle cx="12" cy="12.5" r="4"/>',
 home:'<path d="m3 10 9-7 9 7v11h-7v-7h-4v7H3z"/>',
 arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>',
 'arrow-down':'<path d="M12 4v16m-6-6 6 6 6-6"/>',
 tomato:'<path d="M9 7C2 4 0 15 6 20c4 3 6 1 6 1s2 2 6-1c6-5 4-16-3-13"/><path d="m12 3 1 5 5-2-3 5-3-2-3 2-3-5 5 2Z"/>',
 egg:'<path d="M20 15a8 8 0 0 1-16 0C4 9 8 2 12 2s8 7 8 13Z"/>',
 garlic:'<path d="M11 3h3l-1 5c10 5 9 13-1 13S1 13 10 8zM12 9c-4 5-4 9 0 12m0-12c4 5 4 9 0 12"/>',
 onion:'<path d="M10 3h4l-1 5c10 5 9 13-1 13S1 13 11 8zm2 5c-5 6-5 10 0 13m0-13c5 6 5 10 0 13"/>',
 rice:'<path d="M3 12h18c-1 7-4 9-9 9s-8-2-9-9Zm2-2c-1-5 3-7 6-5 3-4 8-1 8 5M8 7l1 2m5-3 1 2"/>',
 meat:'<path d="M6 6c5-5 10-3 13 1s3 8-1 12S3 23 2 16 3 9 6 6Z"/><path d="M7 8c2-2 6-2 8 0s2 5 0 6-4-1-6 0-5 0-4-3"/>',
 pasta:'<path d="M6 3v8m3-8v8m3-8v8M6 7h6M9 11v10m8-18c5 3 5 7 1 10s-3 5 1 8M16 3c5 3 5 7 1 10s-3 5 1 8"/>',
 heart:'<path d="M20.8 4.6c-2.3-2.3-5.6-1.8-8.8 1.4C8.8 2.8 5.5 2.3 3.2 4.6-1 8.9 4.8 15.3 12 21c7.2-5.7 13-12.1 8.8-16.4Z"/>',
 clock:'<circle cx="12" cy="12" r="9"/><path d="M12 6v6l4 2"/>',
 bookmark:'<path d="M6 3h12v18l-6-4-6 4Z"/>',
 check:'<path d="m5 12 4 4L19 6"/>',
 plus:'<path d="M12 5v14M5 12h14"/>',
 minus:'<path d="M5 12h14"/>',
 x:'<path d="m6 6 12 12M6 18 18 6"/>',
 search:'<circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 5 5"/>',
 lock:'<rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3m-4 4v3"/>',
 info:'<circle cx="12" cy="12" r="9"/><path d="M12 11v6m0-10v1"/>',
 people:'<circle cx="9" cy="7" r="3"/><path d="M3 20v-3a6 6 0 0 1 12 0v3m2-16a3 3 0 0 1 0 6m2 3a5 5 0 0 1 2 7"/>',
 chef:'<path d="M6 13a4 4 0 0 1-2-7 4 4 0 0 1 5-2 4 4 0 0 1 7 0 4 4 0 0 1 4 7l-2 2v8H6Zm0 4h12"/>'
};
const icon = name => `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[name] || icons.leaf}</svg>`;
const $ = (selector,root=document) => root.querySelector(selector);
const $$ = (selector,root=document) => [...root.querySelectorAll(selector)];
const escapeHTML = value => String(value).replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
function hydrateIcons(root=document){$$('[data-icon]',root).forEach(el=>el.innerHTML=icon(el.dataset.icon));}
const aliases = {'pasta':'spaghetti','mince':'ground meat','beef mince':'ground meat','minced beef':'ground meat','ground beef':'ground meat','minced meat':'ground meat','tomato puree':'tomato paste','tomato purée':'tomato paste','egg':'eggs','tomato':'tomatoes','onions':'onion','potatoes':'potato','olive oil':'oil','vegetable oil':'oil','black pepper':'pepper','garlic cloves':'garlic','baby spinach':'spinach'};
const norm = v => {const cleaned=String(v).trim().toLowerCase().replace(/\s+/g,' ').slice(0,60);return aliases[cleaned]||cleaned;};
const title = v => v.charAt(0).toUpperCase()+v.slice(1);
const emojis = {'spaghetti':'🍝','tomato paste':'🥫','ground meat':'🥩','tomatoes':'🍅','eggs':'🥚','spinach':'🥬','onion':'🧅','garlic':'🧄','rice':'🍚','potato':'🥔','cheese':'🧀','bread':'🍞','milk':'🥛','butter':'🧈','oil':'🫒','salt':'🧂','pepper':'🧂'};
const emoji = value => icon(({'spaghetti':'pasta','tomato paste':'tomato','ground meat':'meat','tomatoes':'tomato','eggs':'egg','onion':'onion','garlic':'garlic','rice':'rice'})[value] || 'leaf');
const recipes = [
 {id:'beef-pasta',name:'Your pantry bolognese',kicker:'A WEEKNIGHT LITTLE WIN',description:'Rich tomato, savory mince, and a generous twirl of spaghetti. Comfort, made simple.',image:'pasta',time:25,vegetarian:false,ingredients:[['spaghetti',200,'g'],['tomato paste',3,'tbsp'],['ground meat',250,'g']],basics:['salt','pepper','oil'],steps:[['Get the pasta going','Bring a large pot of water to a boil. Add salt and spaghetti. Cook following the packet timing, then reserve a mug of pasta water before draining.'],['Build a rich sauce','Heat a little oil in a pan over medium heat. Add the ground meat and break it apart. Cook thoroughly; ground beef or pork should reach 71°C, and ground poultry 74°C. Stir in the tomato paste and cook for 2 minutes.'],['Bring it together','Add a splash of reserved pasta water and simmer for 5 minutes, stirring until saucy. Toss in the drained spaghetti. Add more pasta water as needed and season with pepper.']]},
 {id:'tomato-pasta',name:'Simply tomato spaghetti',kicker:'LESS REALLY IS MORE',description:'A silky tomato sauce from a humble spoonful of paste. Your new cupboard classic.',image:'tomato-pasta',time:20,vegetarian:true,ingredients:[['spaghetti',200,'g'],['tomato paste',3,'tbsp']],basics:['salt','oil','pepper'],steps:[['Boil and save a little','Cook spaghetti in boiling salted water according to the packet. Save a mug of the starchy cooking water before draining.'],['Wake up the tomato','Heat a little oil in a pan over medium heat. Add the tomato paste and stir for 2 minutes until it darkens slightly. Stir in enough pasta water to make a smooth sauce.'],['Twirl and serve','Add the spaghetti and toss over gentle heat for a minute. Loosen with more pasta water if needed. Season with pepper and serve warm.']]},
 {id:'green-pasta',name:'Garlicky garden pasta',kicker:'A LITTLE GREEN GOODNESS',description:'Tender greens and golden garlic. A fresh twist on the easiest dinner in the book.',image:'green-pasta',time:20,vegetarian:true,ingredients:[['spaghetti',200,'g'],['spinach',120,'g'],['garlic',2,'cloves']],basics:['salt','oil'],steps:[['Cook the spaghetti','Cook spaghetti in salted boiling water according to the packet. Reserve a mug of pasta water, then drain.'],['Let the greens shine','Thinly slice the garlic. Gently cook it in a little oil for 1 minute without browning. Add washed spinach and stir until just wilted.'],['Toss it all together','Add the spaghetti to the greens. Toss with a splash of pasta water until glossy. Taste for salt and serve.']]},
 {id:'tomato-eggs',name:'Sunny tomato skillet',kicker:'ONE PAN, ALL THE COMFORT',description:'Soft tomatoes and eggs, gently simmered into the coziest little skillet.',image:'salad',time:20,vegetarian:true,ingredients:[['eggs',4,''],['tomatoes',3,''],['onion',0.5,'']],basics:['salt','oil','pepper'],steps:[['Make the tomato base','Finely chop the onion and tomatoes. Soften the onion in a little oil over medium heat for 3 minutes. Add tomatoes and a splash of water; simmer for 7 minutes until saucy.'],['Add the eggs','Make small wells in the sauce and crack in the eggs. Cover the pan and cook gently until both whites and yolks are firm.'],['Finish at the table','Season with salt and pepper, then divide the eggs and sauce between bowls. Serve immediately.']]},
 {id:'egg-rice',name:'Better-at-home egg rice',kicker:'A CLEVER LITTLE LEFTOVER',description:'Fluffy egg, warm rice, and a handful of greens. A satisfying bowl from everyday staples.',image:'rice',time:30,vegetarian:true,ingredients:[['rice',150,'g'],['eggs',2,''],['spinach',80,'g']],basics:['salt','oil'],steps:[['Start with the rice','Cook the dry rice according to its packet instructions, then drain well. Spread it briefly on a plate while you prepare the eggs.'],['Scramble and stir','Heat a little oil in a wide pan. Beat the eggs, add to the pan, and stir until fully set. Add washed spinach and cook until wilted.'],['Bring on the rice','Add the freshly cooked rice and stir-fry for 2–3 minutes until hot throughout. Season with salt. Serve immediately.']]},
 {id:'garlic-rice',name:'Golden garlic rice',kicker:'SMALL INGREDIENT LIST. BIG COMFORT.',description:'Fragrant garlic and onion bring a pot of rice beautifully to life.',image:'rice',time:25,vegetarian:true,ingredients:[['rice',150,'g'],['garlic',2,'cloves'],['onion',0.5,'']],basics:['salt','oil'],steps:[['Soften and sizzle','Dice the onion and mince the garlic. Heat a little oil in a saucepan, soften the onion for 4 minutes, then stir in the garlic for 30 seconds.'],['A little patience','Stir in rinsed rice. Add water in the ratio recommended on its packet and a pinch of salt. Cover and simmer for the packet cooking time.'],['Fluff and enjoy','Remove from heat and rest, covered, for 5 minutes. Fluff with a fork and serve.']]},
 {id:'spinach-eggs',name:'Everyday green scramble',kicker:'A FRESH START, ANY TIME',description:'Soft scrambled eggs folded around bright green spinach. Simple is a good thing.',image:'salad',time:10,vegetarian:true,ingredients:[['eggs',4,''],['spinach',100,'g']],basics:['salt','oil','pepper'],steps:[['Ready your greens','Wash the spinach and drain. Beat the eggs with a pinch of salt and pepper.'],['Wilt and fold','Heat a little oil over medium-low heat. Add the spinach and stir until wilted, then pour in the eggs.'],['Slow and gentle','Use a spatula to gently fold the eggs until fully set. Divide between plates and serve straight away.']]},
 {id:'meat-rice',name:'One-pan tomato mince & rice',kicker:'A LITTLE LESS WASHING UP',description:'A hearty pot of rice and tomato-rich mince. All the flavor, one happy pan.',image:'rice',time:35,vegetarian:false,ingredients:[['ground meat',250,'g'],['rice',150,'g'],['tomato paste',2,'tbsp']],basics:['salt','oil','pepper'],steps:[['Brown the mince','Heat a little oil in a wide pan with a lid. Add ground meat and cook thoroughly, breaking it apart; ground beef or pork should reach 71°C, and ground poultry 74°C. Stir in tomato paste and cook for 1 minute.'],['Let it simmer','Stir in rinsed rice and the amount of water its packet recommends. Add salt, cover, and simmer for the packet cooking time, adding water if needed.'],['Rest and serve','Check that the rice is tender. Turn off the heat and leave covered for 5 minutes, then fluff and season with pepper.']]}
];
const storageKey='pantry-prototype-v1';
let stored={};try{stored=JSON.parse(localStorage.getItem(storageKey)||'{}')||{};}catch{}
const state={ingredients:Array.isArray(stored.ingredients)?[...new Set(stored.ingredients.filter(x=>typeof x==='string').map(norm).filter(Boolean))].slice(0,40):['spaghetti','tomato paste','ground meat'],staples:stored.staples!==false,saved:Array.isArray(stored.saved)?stored.saved.filter(x=>recipes.some(r=>r.id===x)):[],filter:'all',generated:false,currentRecipe:null,servings:2,cookStep:0,billing:'annual',detected:[],editIndex:null,photoUrl:null};
function persist(){try{localStorage.setItem(storageKey,JSON.stringify({ingredients:state.ingredients,staples:state.staples,saved:state.saved}));}catch{toast('Browser storage is unavailable. Changes will last for this visit.');}}
let toastTimer;function toast(message){$('#toast').textContent=message;$('#toast').classList.add('visible');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').classList.remove('visible'),3200);}
function openDialog(id){const dialog=$(id);dialog.showModal();document.body.style.overflow='hidden';}
function closeDialog(dialog){dialog.close();if(!document.querySelector('dialog[open]'))document.body.style.overflow='';}
function ingredientsChanged(){persist();renderPantry();renderResults();renderSaved();$('#pantry-error').textContent='';}
function addIngredients(value){const incoming=value.split(/[,\n]/).map(norm).filter(Boolean);let count=0;for(const item of incoming){if(state.ingredients.length>=40)break;if(!state.ingredients.includes(item)){state.ingredients.push(item);count++;}}if(!incoming.length){$('#pantry-error').textContent='Add an ingredient to get started.';return false;}if(count===0){toast(state.ingredients.length>=40?'Your kitchen can hold up to 40 ingredients.':'That’s already in your kitchen.');return false;}ingredientsChanged();return true;}
function renderPantry(){
 $('#ingredient-count').textContent=state.ingredients.length;
 $('#ingredient-chips').innerHTML=state.ingredients.length?state.ingredients.map((item,index)=>`<div class="ingredient-chip"><button class="edit-chip" data-edit="${index}" aria-label="Edit ${escapeHTML(item)}"><span aria-hidden="true">${emoji(item)}</span>${escapeHTML(title(item))}</button><button class="remove-chip" data-remove="${index}" aria-label="Remove ${escapeHTML(item)}">${icon('x')}</button></div>`).join(''):'<p class="input-hint">Your kitchen is a blank canvas. Add your first ingredient above.</p>';
 const suggestions=['eggs','spinach','garlic','tomatoes','rice','onion'].filter(x=>!state.ingredients.includes(x)).slice(0,4);
 $('#suggestion-chips').innerHTML=suggestions.map(x=>`<button class="suggest-chip" data-suggest="${x}">${icon('plus')}${title(x)}</button>`).join('');
 $('#staples').checked=state.staples;
}
function recipeMatch(recipe){const required=recipe.ingredients.map(x=>x[0]);const missing=required.filter(x=>!state.ingredients.includes(x));const missingBasics=state.staples?[]:recipe.basics.filter(x=>!state.ingredients.includes(x));return {missing:[...missing,...missingBasics],matched:required.length-missing.length,required:required.length,ready:!missing.length&&!missingBasics.length};}
function card(recipe){const match=recipeMatch(recipe);const saved=state.saved.includes(recipe.id);return `<article class="recipe-card"><button class="save-recipe ${saved?'saved':''}" data-save="${recipe.id}" aria-label="${saved?'Unsave':'Save'} ${recipe.name}" aria-pressed="${saved}">${icon('bookmark')}</button><button class="recipe-open" data-recipe="${recipe.id}" aria-label="View ${recipe.name}"><div class="card-photo"><img src="/assets/${recipe.image}.jpg" alt="Illustrative ${recipe.image.includes('pasta')?'pasta dish':recipe.image==='rice'?'meal bowl':'vegetable dish'}" loading="lazy"><span class="match-badge ${match.ready?'':'missing'}">${icon(match.ready?'check':'plus')}${match.ready?'You have what it takes':`${match.missing.length} more ingredient${match.missing.length===1?'':'s'}`}</span></div><div class="card-body"><div class="card-kicker">${recipe.kicker}</div><h3>${recipe.name}</h3><p>${recipe.description}</p><div class="card-meta"><span>${icon('clock')}${recipe.time} min</span><span class="dot"></span><span>${recipe.vegetarian?'Vegetarian':'Easy'}</span><i>${icon('arrow')}</i></div></div></button></article>`;}
function renderResults(){
 let matches=recipes.map(r=>({r,m:recipeMatch(r)})).filter(x=>x.m.matched>0).sort((a,b)=>Number(b.m.ready)-Number(a.m.ready)||a.m.missing.length-b.m.missing.length||b.m.matched-a.m.matched);
 if(state.filter==='quick')matches=matches.filter(x=>x.r.time<=30);
 if(state.filter==='vegetarian')matches=matches.filter(x=>x.r.vegetarian);
 const ready=matches.filter(x=>x.m.ready).length;
 $('#results-eyebrow').textContent=state.generated?'MADE FOR YOUR KITCHEN':'YOUR NEXT GOOD MEAL';
 $('#results-title').textContent=state.generated?(ready?`${ready} recipe${ready===1?'':'s'}. Already in your kitchen.`:'A little closer to dinner.'):'A little dinner inspiration.';
 $('#results-description').textContent=state.generated?(ready?'Ready-to-cook recipes come first. Other ideas show what’s missing.':'These sample recipes are the closest matches. Check what you still need.'):'A taste of what’s possible with your ingredients.';
 $('#recipe-grid').innerHTML=matches.length?matches.slice(0,state.generated?8:3).map(x=>card(x.r)).join(''):`<div class="empty-state"><i>${icon('leaf')}</i><h3>${state.ingredients.length?'A little room for inspiration.':'Start with what you have.'}</h3><p>${state.ingredients.length?'No sample recipes match this pantry and filter yet. Try eggs, rice or tomatoes, or explore the sample kitchen.':'Add your ingredients above, or try our sample kitchen to discover a few delicious possibilities.'}</p><button class="button secondary" data-action="sample">Try a sample kitchen ${icon('arrow')}</button></div>`;
 $$('.filter').forEach(el=>{el.classList.toggle('active',el.dataset.filter===state.filter);el.setAttribute('aria-pressed',el.dataset.filter===state.filter);});
}
function renderSaved(){
 $('#saved-count').textContent=state.saved.length;
 const saved=recipes.filter(x=>state.saved.includes(x.id));
 $('#saved-grid').innerHTML=saved.length?saved.map(card).join(''):`<div class="empty-state"><i>${icon('bookmark')}</i><h3>A home for your go-to meals.</h3><p>Tap the bookmark on a recipe to keep it here. Your favorites stay saved in this browser.</p><button class="button primary" data-nav="kitchen">Find something delicious ${icon('arrow')}</button></div>`;
}
function toggleSave(id){const was=state.saved.includes(id);state.saved=was?state.saved.filter(x=>x!==id):[...state.saved,id];persist();renderResults();renderSaved();if(state.currentRecipe?.id===id){const btn=$('[data-detail-save]');if(btn){btn.classList.toggle('saved',!was);btn.setAttribute('aria-pressed',!was);btn.setAttribute('aria-label',was?'Save recipe':'Unsave recipe');}}toast(was?'Recipe removed from your recipe book.':'A good one, saved to My recipes.');}
function navigate(view){const saved=view==='recipes';$('#kitchen-view').hidden=saved;$('#saved-view').hidden=!saved;$('#breadcrumb-current').textContent=saved?'My recipes':'My kitchen';$$('[data-nav]').forEach(el=>el.classList.toggle('active',el.dataset.nav===view));if(saved)renderSaved();window.scrollTo({top:0,behavior:'smooth'});}
function startScan(url=null){state.detected=['tomatoes','eggs','spinach','onion','rice','garlic'];$('#review-photo').innerHTML=url?`<img src="${url}" alt="Your selected ingredient photo, kept in this browser">`:`<span class="sample-produce" aria-label="Sample tomatoes, eggs, spinach and garlic">${['tomato','egg','leaf','garlic'].map(x=>'<i>'+icon(x)+'</i>').join('')}</span>`;$('#scan-note').textContent=url?'Demo scan: these suggestions are sample data, not a reading of your photo. Your photo stays in this browser.':'Demo scan: these are sample ingredients, not AI detections. Try editing one before adding them.';renderDetected();openDialog('#capture-dialog');}
function renderDetected(){ $('#detected-list').innerHTML=state.detected.map((x,i)=>`<div class="detected-row"><span aria-hidden="true">${emoji(norm(x))}</span><input aria-label="Detected ingredient ${i+1}" data-detected="${i}" maxlength="60" value="${escapeHTML(title(x))}"><button class="icon-button" data-remove-detected="${i}" aria-label="Remove detected ${escapeHTML(x)}">${icon('x')}</button></div>`).join('');$('#confirm-detected').disabled=!state.detected.some(x=>norm(x));$('#confirm-detected').innerHTML=`Add to my kitchen ${icon('arrow')}`;}
function amount(n){const val=n*state.servings/2;return Number.isInteger(val)?String(val):String(Math.round(val*100)/100);}
function renderRecipe(){const r=state.currentRecipe;if(!r)return;const match=recipeMatch(r);const saved=state.saved.includes(r.id);$('#recipe-dialog').innerHTML=`<div class="detail-photo"><img src="/assets/${r.image}.jpg" alt="Illustrative food photography"><span class="photo-credit">Illustrative photo · Sample recipe</span><button class="icon-button" data-close aria-label="Close recipe">${icon('x')}</button></div><div class="detail-body"><div class="eyebrow muted">${r.kicker}</div><div class="detail-heading"><h2 id="detail-title">${r.name}</h2><button class="icon-button ${saved?'saved':''}" data-detail-save="${r.id}" aria-label="${saved?'Unsave':'Save'} recipe" aria-pressed="${saved}">${icon('bookmark')}</button></div><p class="detail-description">${r.description}</p><div class="detail-metadata"><span>${icon('clock')}${r.time} minutes</span><span>${icon('chef')}Easy</span><span>${icon(r.vegetarian?'leaf':'people')}${r.vegetarian?'Vegetarian':`${state.servings} servings`}</span></div>${match.ready?'<div class="notice">'+icon('check')+'<span>You have all the listed ingredients. Check your quantities below before you begin.</span></div>':'<div class="notice">'+icon('info')+`<span>Still needed: <strong>${match.missing.map(title).map(escapeHTML).join(', ')}</strong>. These are required for this recipe.</span></div>`}<div class="detail-columns"><div><div class="ingredient-title-row"><h3>Ingredients</h3><div class="servings-control"><button data-servings="-1" aria-label="Fewer servings" ${state.servings<=1?'disabled':''}>${icon('minus')}</button><span>${state.servings} servings</span><button data-servings="1" aria-label="More servings" ${state.servings>=8?'disabled':''}>${icon('plus')}</button></div></div>${r.ingredients.map(([name,n,unit])=>`<label class="detail-ingredient"><input type="checkbox" data-check-ingredient><span class="ingredient-text">${amount(n)}${unit?' '+unit:''} ${escapeHTML(name)}</span>${state.ingredients.includes(name)?'':'<small>Need this</small>'}</label>`).join('')}<p class="staples-note"><strong>Also needed:</strong> ${r.basics.join(', ')}. Use a little oil and season to taste. Water is used where the method calls for it.</p><p class="staples-note">Recipe quantities are suggestions; your pantry stores ingredient names, not stock amounts.</p></div><div><h3 class="method-title">A little step by step.</h3>${r.steps.map((step,i)=>`<div class="method-step"><span class="step-number">${i+1}</span><p><strong>${step[0]}</strong>${step[1]}</p></div>`).join('')}</div></div><div class="detail-bottom"><p>Put on something good.<br>Let’s make dinner.</p><button class="button primary" data-action="start-cooking">${icon('chef')}Start cooking ${icon('arrow')}</button></div></div>`;$('#recipe-dialog').setAttribute('aria-labelledby','detail-title');}
function renderCooking(){const r=state.currentRecipe;const done=state.cookStep>=r.steps.length;$('#recipe-dialog').innerHTML=`<div class="detail-body"><div class="modal-top"><span class="eyebrow muted">${escapeHTML(r.name)}</span><button class="icon-button" data-close aria-label="Close cooking mode">${icon('x')}</button></div>${done?`<div class="cook-finished"><i>${icon('check')}</i><h2 id="cook-title">Made something good.</h2><p>A few ingredients. A meal to be proud of.<br>Enjoy every bite.</p><button class="button primary" data-action="finish-cooking">Back to my kitchen ${icon('arrow')}</button></div>`:`<div class="cook-progress">${r.steps.map((_,i)=>`<span class="${i<=state.cookStep?'done':''}"></span>`).join('')}</div><div class="cook-step"><span class="cook-label">Step ${state.cookStep+1} of ${r.steps.length} · ${state.servings} servings</span><h2 id="cook-title">${r.steps[state.cookStep][0]}</h2><p>${r.steps[state.cookStep][1]}</p></div><div class="cook-actions"><button class="button secondary" data-action="cook-prev">${state.cookStep?'Previous step':'View recipe'}</button><button class="button primary" data-action="cook-next">${state.cookStep===r.steps.length-1?'Finish cooking':'Next step'} ${icon('arrow')}</button></div>`}</div>`;$('#recipe-dialog').setAttribute('aria-labelledby','cook-title');$('#recipe-dialog').scrollTop=0;}
function renderPro(){ /* Replaced by the account-backed Premium module. */ }
function updatePlanLabel(){$('#plan-label').textContent='Free plan';}
document.addEventListener('click',event=>{
 const button=event.target.closest('button,a');if(!button)return;
 if(button.hasAttribute('data-close')){const dialog=button.closest('dialog');if(dialog)closeDialog(dialog);return;}
 if(button.dataset.nav){navigate(button.dataset.nav);return;}
 if(button.dataset.suggest){addIngredients(button.dataset.suggest);return;}
 if(button.dataset.remove!==undefined){state.ingredients.splice(Number(button.dataset.remove),1);ingredientsChanged();return;}
 if(button.dataset.edit!==undefined){state.editIndex=Number(button.dataset.edit);$('#edit-input').value=title(state.ingredients[state.editIndex]);openDialog('#edit-dialog');$('#edit-input').focus();return;}
 if(button.dataset.removeDetected!==undefined){state.detected.splice(Number(button.dataset.removeDetected),1);renderDetected();return;}
 if(button.dataset.filter){state.filter=button.dataset.filter;renderResults();return;}
 if(button.dataset.recipe){state.currentRecipe=recipes.find(x=>x.id===button.dataset.recipe);state.servings=2;renderRecipe();openDialog('#recipe-dialog');return;}
 if(button.dataset.save||button.dataset.detailSave){toggleSave(button.dataset.save||button.dataset.detailSave);return;}
 if(button.dataset.servings){state.servings=Math.min(8,Math.max(1,state.servings+Number(button.dataset.servings)));renderRecipe();return;}
 switch(button.dataset.action){
  case 'capture':$('#photo-input').click();break;
  case 'sample':startScan();break;
  case 'clear':state.ingredients=[];ingredientsChanged();toast('A fresh start. Add what’s in your kitchen.');break;
  case 'open-pro':renderPro();openDialog('#pro-dialog');break;
  case 'start-cooking':state.cookStep=0;renderCooking();break;
  case 'cook-next':state.cookStep++;renderCooking();break;
  case 'cook-prev':if(state.cookStep===0)renderRecipe();else{state.cookStep--;renderCooking();}break;
  case 'finish-cooking':closeDialog($('#recipe-dialog'));navigate('kitchen');toast('Dinner, made from what you had. Enjoy!');break;
 }
});
$('#ingredient-form').addEventListener('submit',event=>{event.preventDefault();if(addIngredients($('#ingredient-input').value))$('#ingredient-input').value='';$('#ingredient-input').focus();});
$('#staples').addEventListener('change',event=>{state.staples=event.target.checked;ingredientsChanged();});
$('#find-recipes').addEventListener('click',()=>{if(!state.ingredients.length){$('#pantry-error').textContent='Add at least one ingredient, or try a sample photo.';$('#ingredient-input').focus();return;}state.generated=true;renderResults();$('#flow-discover').classList.add('current');$('#results').scrollIntoView({behavior:'smooth',block:'start'});$('#results-title').setAttribute('tabindex','-1');$('#results-title').focus({preventScroll:true});});
$('#edit-form').addEventListener('submit',event=>{event.preventDefault();const value=norm($('#edit-input').value);if(!value)return;if(state.ingredients.includes(value)&&state.ingredients.indexOf(value)!==state.editIndex){toast('That ingredient is already in your kitchen.');return;}state.ingredients[state.editIndex]=value;ingredientsChanged();closeDialog($('#edit-dialog'));});
$('#detected-list').addEventListener('input',event=>{if(event.target.dataset.detected!==undefined){state.detected[Number(event.target.dataset.detected)]=event.target.value;$('#confirm-detected').disabled=!state.detected.some(x=>norm(x));}});
$('#detected-form').addEventListener('submit',event=>{event.preventDefault();const val=norm($('#detected-input').value);if(!val)return;if(state.detected.map(norm).includes(val)){toast('That suggestion is already in the list.');return;}if(state.detected.length>=20){toast('Review up to 20 ingredients at a time.');return;}state.detected.push(val);$('#detected-input').value='';renderDetected();});
$('#confirm-detected').addEventListener('click',()=>{const before=state.ingredients.length;addIngredients(state.detected.join(','));closeDialog($('#capture-dialog'));navigate('kitchen');$('#pantry-workspace').scrollIntoView({behavior:'smooth'});const added=state.ingredients.length-before;toast(added?`${added} ingredient${added===1?'':'s'} added. Your kitchen is looking good.`:'Those ingredients are already in your kitchen.');});
$('#photo-input').addEventListener('change',async event=>{const file=event.target.files[0];if(!file)return;if(!/^image\/(jpeg|png|webp|gif|avif)$/.test(file.type)){toast('Please choose a JPG, PNG, WebP, GIF or AVIF image.');event.target.value='';return;}if(file.size>10*1024*1024){toast('Choose an image smaller than 10 MB.');event.target.value='';return;}if(state.photoUrl)URL.revokeObjectURL(state.photoUrl);const url=URL.createObjectURL(file);try{const image=new Image();image.src=url;await image.decode();state.photoUrl=url;startScan(url);}catch{URL.revokeObjectURL(url);toast('That image could not be opened. Please try another.');}event.target.value='';});
document.addEventListener('change',event=>{if(event.target.hasAttribute('data-check-ingredient'))event.target.closest('label').classList.toggle('checked',event.target.checked);});
$$('dialog').forEach(dialog=>{dialog.addEventListener('close',()=>{if(!document.querySelector('dialog[open]'))document.body.style.overflow='';});dialog.addEventListener('click',event=>{if(event.target!==dialog)return;const rect=dialog.getBoundingClientRect();if(event.clientX<rect.left||event.clientX>rect.right||event.clientY<rect.top||event.clientY>rect.bottom)closeDialog(dialog);});});
$('#capture-dialog').setAttribute('aria-label','Review sample detected ingredients');$('#edit-dialog').setAttribute('aria-label','Edit an ingredient');
hydrateIcons();renderPantry();renderResults();renderSaved();updatePlanLabel();
